namespace EduTech.Shared.Constants;

/// <summary>Student gender — a fixed, closed set. Stored as the snake_case string on <c>students.gender</c>.</summary>
public enum Gender
{
    Male,
    Female
}
